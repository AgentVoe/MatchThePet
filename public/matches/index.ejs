<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Matches</title>
</head>
<body>
<% if (pets.length) { %>
    <ul>
        <% pets.forEach((pet) => { %>
            <li><%= pet.breed %></li>
        <% }) %>
    </ul>
<% } else { %>
    <p>No pets found.</p>
<% } %>
</body>
</html>
